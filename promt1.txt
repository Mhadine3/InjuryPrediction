You are a senior data architect specialized in sport science applications, with deep expertise in:
- PostgreSQL 16+ and TimescaleDB for time-series workloads
- Modeling physiological data of professional athletes (internal/external load, wellness, recovery)
- Industry standards used by Smartabase, Catapult, and Kitman Labs
- Scientific literature: Foster 2001 (sRPE), Hooper 1995 (wellness questionnaire), Gabbett 2016 (ACWR), Buchheit 2014 (HRV monitoring)
- Multi-tenant architecture for coach/athlete applications

PROJECT CONTEXT:
Cross-platform application (web + mobile + tablet) for professional athletes and their coaches.
Target: 4 teams from Group C of the 2026 FIFA World Cup (Brazil, Morocco, Haiti, Scotland).
Volume: 100 players (25 per team, 3rd goalkeeper excluded) × 90 days × 15 daily metrics = ~135,000 measurement rows.
Differentiating feature: injury prediction via ACWR (Acute:Chronic Workload Ratio) and XGBoost ML model.
Multi-coach: several coaches (strength & conditioning, tactical, mental, goalkeeper coach) must share the planning for the same athlete to prevent overtraining.

FIXED TECHNICAL STACK:
- PostgreSQL 16 with TimescaleDB extension for hypertables on daily metrics
- Python 3.11 FastAPI backend
- Next.js 14 (App Router) frontend for web
- React Native Expo SDK 51 for mobile
- Docker Compose deployment on Ubuntu 24.04 VPS

EXPECTED DELIVERABLES IN THIS RESPONSE — 3 complete files, production-ready:

DELIVERABLE 1 — schema.sql
Complete SQL file with all CREATE TABLE statements, constraints, indexes, and TimescaleDB hypertables.
Minimum required tables:
- teams (national teams)
- players (athletes with FK to teams)
- coaches (staff members with specialty: physical / tactical / mental / goalkeeper)
- player_coach_assignments (multi-coach per athlete relationship)
- player_baseline_profiles (physiological reference profile — one per player)
- competitions (World Cup 2026, friendlies, etc.)
- matches (scheduled and played matches)
- training_sessions (sessions planned by coaches)
- daily_metrics (TIMESCALEDB HYPERTABLE — 15 metrics per day per player)
- wellness_questionnaires (daily Hooper responses)
- injury_history (past injuries)
- injury_predictions (ML model outputs — ACWR, risk score, alerts)
- audit_log (traceability of coach actions)

For each table provide: primary key, foreign keys, appropriate types (UUID where relevant), NOT NULL constraints, CHECK constraints on physiological ranges (e.g., HRV between 20 and 150 ms, RPE between 0 and 10), indexes on frequently queried columns, SQL COMMENT statements on every table and every non-obvious column.

The daily_metrics hypertable must be partitioned by date (chunk_time_interval = 7 days) and include the following 15 metrics as columns:
- sleep_duration_h, sleep_quality, fatigue, soreness, stress (Hooper wellness)
- session_distance_km, high_intensity_distance_m, sprints_count, accel_decel_count (GPS external load)
- rpe, session_duration_min, srpe (Foster internal load)
- hrv_ms, resting_hr_bpm, hydration_usg (Buchheit recovery)

DELIVERABLE 2 — Repository structure (ASCII tree)
Mono-repo organized with the following main folders: backend/, web/, mobile/, ml/, data/, docs/, scripts/.
Include main configuration files (docker-compose.yml, .env.example, .gitignore, README.md at root).
For each major folder, indicate its role and expected subfolders (no need to write the actual code — just the tree structure and a one-line comment per folder).

DELIVERABLE 3 — scientific_tables.json
A JSON file containing all scientifically-sourced modulator tables, directly consumable by the Python data generation script. Expected structure:

{
  "metadata": { "version", "generated_at", "sources" },
  "session_types": { ... RPE distributions and durations per session type, source Foster 2001 ... },
  "wellness_modulators": { ... wellness adjustments based on calendar context, source Hooper 1995 ... },
  "recovery_modulators": { ... HRV and resting HR post-match adjustments, source Buchheit 2014 ... },
  "gps_load_by_position": { ... distance, sprints, accelerations per playing position, sources Dellal 2010 and Bradley 2009 ... },
  "acwr_thresholds": { ... ACWR injury risk thresholds, source Gabbett 2016 ... },
  "physiological_ranges": { ... realistic min/max ranges for each metric ... }
}

Every value must be either commented inline or referenced in the "sources" section with author/year/journal.

IMPORTANT CONSTRAINTS:
- Everything must be ready to execute without modification
- The SQL must run successfully on PostgreSQL 16 with TimescaleDB extension
- The JSON must be valid (directly parseable)
- Technical choices (column types, partitioning strategy, indexes) must be briefly justified in comments
- Think long-term: this schema must support scaling to 48 teams later without major refactor

No questions, no requests for clarification. You have all the information needed. Deliver the 3 complete files in this response, in the order specified above, separated by clear headers.